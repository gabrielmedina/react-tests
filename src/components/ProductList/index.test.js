import React from 'react'
import ProductList from './'

describe('Testes para produtos', () => {
  it('Deve adicionar um novo produto na lista quando o formulário for submetido', () => {
    // renderizar o componente
    // buscar pelo input
    // digitar o nome do novo produto no input
    // buscar pelo botão de submissão
    // clicar no botão
    // buscar a lista de produtos
    // verificar se o produto foi adicionado
  })
})